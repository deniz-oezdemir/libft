/*
do NOT submit this file!
*/

#include "libft.h"
#include <stdio.h>

int	main()
{
	printf("u is printable: %d\n", ft_isprint('u'));
	printf("length of hello: %zu\n", ft_strlen("hello"));
	printf("only the word in the middle of hello my friend: %s\n", ft_substr("hello my friend", 6, 2));
	return (0);
}
